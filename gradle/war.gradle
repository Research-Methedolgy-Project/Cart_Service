apply plugin: "war"

bootWar {
    mainClassName = "com.mycompany.myapp.CartServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
